name: Production hardening

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - agent/mongolian-copy-audit-fixes

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: node --check app.js
      - run: npm test
        env:
          CROSS_PROJECT_FORBIDDEN_TOKEN: ${{ secrets.CROSS_PROJECT_FORBIDDEN_TOKEN }}
      - run: npm run test:e2e
      - run: npm run test:contracts
      - run: npm run verify:production-package
        env:
          CROSS_PROJECT_FORBIDDEN_TOKEN: ${{ secrets.CROSS_PROJECT_FORBIDDEN_TOKEN }}
      - run: npm run verify:database-config
      - run: npm run verify:recovery-config
      - run: npm run verify:qpay-config
      - run: npm run verify:domain-config
        env:
          CROSS_PROJECT_FORBIDDEN_TOKEN: ${{ secrets.CROSS_PROJECT_FORBIDDEN_TOKEN }}
      - run: npm run build:staging
      - run: npm run verify:staging-package
        env:
          CROSS_PROJECT_FORBIDDEN_TOKEN: ${{ secrets.CROSS_PROJECT_FORBIDDEN_TOKEN }}
      - run: git diff --exit-code
      - run: test -z "$(git status --porcelain)"
