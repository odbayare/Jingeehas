Deploy current main to the linked Jingeehas Netlify production site and certify the exact live candidate package.

Release scope: migrate the optional personalized full report to the authoritative one-time 19,900₮ offer while preserving the free assessment and initial result, sealed pre-payment content, safety bypass, report engine, entitlement semantics, and recovery behavior. New invoices use 19,900₮. Historical server-created 9,900₮ and 39,000₮ payments remain verifiable from stored payment.amount, and unpaid stale legacy invoices are never shown as a current offer. Browser/CAPI Purchase uses the persisted transaction amount. The production DB change is expand-only and must leave all historical payment and entitlement rows unchanged.
Source: merge the single approved price-migration PR, then deploy that exact resulting main SHA.
Expected live origin: https://jingeehas.fit
Expected deployment package: dist static assets plus .generated-copy-hotfix/netlify/functions
Required certification: all unit/contract/E2E gates pass; exact candidate/live manifest match; /assessment/result renders the optional 19,900₮ one-time price and CTA "БҮРЭН ТАЙЛАНГАА НЭЭХ"; old coffee/cheapness copy is absent; safety routes bypass commerce; new QPay invoices use 19,900₮; historical 9,900₮ and 39,000₮ payment verification remains supported; entitlement/report access remains intact; Meta Purchase/checkout value reflects the actual stored payment amount; no personalized pattern names, counts, scores, answers, or report content leak before payment or into tracking metadata. Record the new Netlify published timestamp as P19900_T0 after deploy.
