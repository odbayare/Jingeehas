Deploy current main to the linked Jingeehas Netlify production site and certify the exact live candidate package.

Release scope: PR #53 fixes the admin-panel report-candidate N+1 bottleneck. The deploy-gate test repairs now keep both owner-preview and recovery contract sessions on their own synthetic clocks, preventing historical test timestamps from expiring against the real CI clock. These are test-only changes; production auth, session, recovery, payment, assessment, and report behavior are unchanged.
Source performance fix merge SHA: 71030beea6ecc1e5e58fc99df0d1a983e9a333bc
Expected live origin: https://jingeehas.fit
Expected deployment package: dist static assets plus .generated-copy-hotfix/netlify/functions
Required certification: all unit/contract/E2E gates pass; exact candidate/live manifest match; admin report candidate endpoint uses entitlement-first filtering, batched legacy snapshot reads, and bounded version lookup concurrency; no per-completed-assessment serial N+1 path; admin authentication remains protected; analytics/question-progress remain correct; public assessment, recovery, payment/QPay, safety, report rendering, report activation, routes, static assets, and generated functions remain unchanged and pass existing production smoke gates.
